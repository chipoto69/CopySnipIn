---
phase: 01-executable-scaffold-factory-portability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .python-version
  - pyproject.toml
  - uv.lock
  - src/copysnipin/__init__.py
  - src/copysnipin/py.typed
  - tests/copysnipin/test_imports.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
  - FOUND-05
must_haves:
  truths:
    - "Developer can run uv sync from tracked package metadata and uv.lock."
    - "Developer can import the copysnipin package from src/copysnipin."
    - "Developer can run pytest, mypy, Ruff check, and Ruff format-check against the initial scaffold."
  artifacts:
    - path: "pyproject.toml"
      provides: "Python 3.13+ package metadata, dependencies, scripts, and tool config"
      contains: "requires-python = \">=3.13\""
    - path: "uv.lock"
      provides: "uv-managed reproducible lockfile"
    - path: "src/copysnipin/py.typed"
      provides: "PEP 561 typed-package marker"
    - path: "tests/copysnipin/test_imports.py"
      provides: "Mirrored package import smoke coverage"
  key_links:
    - from: "pyproject.toml"
      to: "src/copysnipin"
      via: "Hatchling wheel package config"
      pattern: "packages = \\[\"src/copysnipin\"\\]"
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv lock / uv sync --locked"
      pattern: "requires-python = \">=3.13\""
---

<objective>
Create the Python package and toolchain substrate for CopySnipIn.

Purpose: Phase 1 cannot execute until the repository has tracked package metadata,
a lockfile, a source package, typed-package marker, and initial test coverage.
Output: A `uv`-managed Python 3.13+ package with import smoke tests and quality
gate configuration.
</objective>

<execution_context>
@/Users/rudlord/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rudlord/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-executable-scaffold-factory-portability/01-CONTEXT.md
@.planning/phases/01-executable-scaffold-factory-portability/01-RESEARCH.md
@.planning/phases/01-executable-scaffold-factory-portability/01-VALIDATION.md
@.planning/phases/01-executable-scaffold-factory-portability/01-PATTERNS.md
@AGENTS.md

<interfaces>
This plan creates the base package contract downstream plans will use:

```python
# src/copysnipin/__init__.py
__version__: str = "0.1.0"
__all__ = ["__version__"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create uv package metadata and Python selector</name>
  <files>.python-version, pyproject.toml</files>
  <read_first>
    - .planning/phases/01-executable-scaffold-factory-portability/01-CONTEXT.md
    - .planning/phases/01-executable-scaffold-factory-portability/01-RESEARCH.md
    - .planning/phases/01-executable-scaffold-factory-portability/01-PATTERNS.md
    - AGENTS.md
    - .python-version if it exists
    - pyproject.toml if it exists
  </read_first>
  <action>
    Create `.python-version` containing exactly `3.13`.

    Create `pyproject.toml` with:
    - `[build-system]` using `hatchling>=1.29,<2` and `hatchling.build`.
    - `[project]` name `copysnipin`, version `0.1.0`, `requires-python = ">=3.13"`.
    - Runtime dependencies: `fastapi>=0.136,<0.137`, `uvicorn[standard]>=0.45,<0.46`, and `textual>=8.2,<9`.
    - Console scripts per D-05/D-07: `copysnipin-api`, `copysnipin-scanner`, `copysnipin-tracker`, `copysnipin-simulator`, `copysnipin-pyth-feed`, and `copysnipin-dashboard`.
    - `[dependency-groups].dev` containing `pytest>=9,<10`, `mypy>=1.20,<2`, `ruff>=0.15,<0.16`, and `httpx>=0.28,<0.29`.
    - `[tool.hatch.build.targets.wheel] packages = ["src/copysnipin"]`.
    - `[tool.pytest.ini_options] testpaths = ["tests"]` and `pythonpath = ["src"]`.
    - `[tool.ruff] line-length = 88`, `target-version = "py313"`, `src = ["src", "tests"]`.
    - `[tool.ruff.lint] select = ["E", "F", "I", "UP", "B"]`.
    - `[tool.mypy] python_version = "3.13"`, `packages = ["copysnipin"]`, `warn_unused_ignores = true`, `warn_return_any = true`, and `disallow_untyped_defs = true`.

    Do not add database, Redis, provider, private-key, signer, order-placement, bridge, funding, relayer, Helius, LaserStream, Jito, or live execution dependencies per D-08 and D-17.
  </action>
  <verify>
    <automated>test "$(cat .python-version)" = "3.13" && grep -q 'requires-python = ">=3.13"' pyproject.toml && grep -q 'copysnipin-scanner = "copysnipin.scanner:main"' pyproject.toml && grep -q 'packages = \["src/copysnipin"\]' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `.python-version` exists and contains only `3.13`.
    - `pyproject.toml` declares `copysnipin` version `0.1.0` and Python `>=3.13`.
    - `pyproject.toml` declares all six console scripts named in D-05 and D-07.
    - `pyproject.toml` contains pytest, mypy, Ruff, and Hatchling configuration in the exact sections listed above.
    - `rg -n "py_clob|private.?key|signer|place_order|cancel_order|bridge|relayer|jito|laserstream" pyproject.toml` returns no matches.
  </acceptance_criteria>
  <done>Package metadata and interpreter selector are present and grep-verifiable.</done>
</task>

<task type="auto">
  <name>Task 2: Create base package and mirrored import test</name>
  <files>src/copysnipin/__init__.py, src/copysnipin/py.typed, tests/copysnipin/test_imports.py</files>
  <read_first>
    - pyproject.toml
    - .planning/phases/01-executable-scaffold-factory-portability/01-CONTEXT.md
    - .planning/phases/01-executable-scaffold-factory-portability/01-PATTERNS.md
    - src/copysnipin/__init__.py if it exists
    - src/copysnipin/py.typed if it exists
    - tests/copysnipin/test_imports.py if it exists
  </read_first>
  <action>
    Create `src/copysnipin/__init__.py` with a typed `__version__: str = "0.1.0"` and `__all__ = ["__version__"]`.

    Create an empty `src/copysnipin/py.typed` marker.

    Create `tests/copysnipin/test_imports.py` with pytest tests that:
    - Import `copysnipin`.
    - Assert `copysnipin.__version__ == "0.1.0"`.
    - Assert `importlib.metadata.version("copysnipin") == "0.1.0"` when run through the uv project environment.

    Keep the test under `tests/copysnipin/` to satisfy FOUND-05's mirrored test tree. Do not add scanner/tracker/simulator/Pyth/dashboard business tests in this plan; those entry points are created in Plan 02.
  </action>
  <verify>
    <automated>test -f src/copysnipin/__init__.py && test -f src/copysnipin/py.typed && test -f tests/copysnipin/test_imports.py && grep -q '__version__: str = "0.1.0"' src/copysnipin/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/copysnipin/__init__.py` exports exactly the package version contract shown in this plan's interfaces block.
    - `src/copysnipin/py.typed` exists.
    - `tests/copysnipin/test_imports.py` exists under the mirrored `tests/copysnipin/` tree.
    - The import test contains assertions for both `copysnipin.__version__` and installed metadata version `0.1.0`.
  </acceptance_criteria>
  <done>Base package import behavior is present and covered by a mirrored pytest file.</done>
</task>

<task type="auto">
  <name>Task 3: Generate uv.lock and prove initial quality gates</name>
  <files>uv.lock</files>
  <read_first>
    - pyproject.toml
    - .python-version
    - src/copysnipin/__init__.py
    - tests/copysnipin/test_imports.py
    - uv.lock if it exists
  </read_first>
  <action>
    Run `uv lock` from the repository root to generate `uv.lock`; do not hand-edit the lockfile.

    Then run `uv sync --locked` to prove FOUND-01. Run the initial quality gates from FOUND-03:
    `uv run pytest`, `uv run mypy src/`, `uv run ruff check .`, and `uv run ruff format --check .`.

    If lock generation updates dependency versions inside `uv.lock`, keep the generated result as-is. If a command fails because a package range from Phase 1 research is unavailable, adjust only the affected version range in `pyproject.toml` to the nearest compatible current major range, regenerate `uv.lock`, and record the change in the plan summary.
  </action>
  <verify>
    <automated>uv sync --locked && uv run pytest && uv run mypy src/ && uv run ruff check . && uv run ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `uv.lock` exists and is generated by `uv lock`.
    - `uv sync --locked` exits 0 from the repository root.
    - `uv run pytest` exits 0 and collects `tests/copysnipin/test_imports.py`.
    - `uv run mypy src/` exits 0.
    - `uv run ruff check .` exits 0.
    - `uv run ruff format --check .` exits 0.
  </acceptance_criteria>
  <done>Tracked package metadata and lockfile support uv sync and initial quality gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer shell -> uv resolver | Local commands fetch package metadata and install dependencies. |
| Package metadata -> future entry points | Console scripts define what commands users can run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Elevation of Privilege | `pyproject.toml` dependencies/scripts | mitigate | Do not add execution-capable trading SDKs, signer/private-key deps, order-placement scripts, bridge/funding/relayer scripts, or live trading command names. Verify with banned-term grep in Task 1. |
| T-1-02 | Tampering | Package install root | accept | This plan does not modify factory root discovery; Plan 03 mitigates wrong-checkout factory commands before factory install/start commands are accepted. |
| T-1-03 | Information Disclosure | Health reporting | accept | No health endpoint is created in this plan; Plan 02 mitigates false health. |
| T-1-04 | Information Disclosure | `uv sync` / tests | mitigate | This plan does not read `.env`; tests only import package metadata and must not print environment values. |
</threat_model>

<verification>
Overall plan verification:

```bash
uv sync --locked
uv run pytest tests/copysnipin/test_imports.py -x
uv run mypy src/
uv run ruff check .
uv run ruff format --check .
```
</verification>

<success_criteria>
FOUND-01 is satisfied by tracked `pyproject.toml` and generated `uv.lock`.
FOUND-03 is partially satisfied by passing scaffold quality gates.
FOUND-05 is started by the committed `tests/copysnipin/` import smoke tree.
</success_criteria>

<output>
After completion, create `.planning/phases/01-executable-scaffold-factory-portability/01-01-SUMMARY.md`.
</output>
