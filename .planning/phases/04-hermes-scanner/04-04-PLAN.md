---
phase: 04-hermes-scanner
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03-PLAN.md
files_modified:
  - src/copysnipin/hermes_scanner/notifications.py
  - src/copysnipin/hermes_scanner/service.py
  - docs/validation-index.md
  - tests/copysnipin/test_scanner_notifications.py
  - tests/copysnipin/test_validation_index.py
autonomous: true
requirements:
  - SCAN-06
  - SCAN-07
must_haves:
  truths:
    - "Discord and Telegram alerts are sent only after qualifying wallet persistence succeeds."
    - "Notification failures are recorded and never fail the scan cycle."
    - "`docs/validation-index.md` points every `VAL-SCAN-*` row to concrete Phase 04 evidence."
  artifacts:
    - path: "src/copysnipin/hermes_scanner/notifications.py"
      provides: "Notification sender protocols and fakeable dispatch helpers"
    - path: "tests/copysnipin/test_scanner_notifications.py"
      provides: "Post-persistence alert and failure-isolation coverage"
    - path: "docs/validation-index.md"
      provides: "Updated scanner validation evidence mapping"
  key_links:
    - from: "src/copysnipin/hermes_scanner/service.py"
      to: "src/copysnipin/repositories/notifications.py"
      via: "durable notification attempt records"
      pattern: "record_notification_attempt"
---

<objective>
Finish scanner notifications, validation evidence, and full Phase 04 gates.

Purpose: Phase 04 must expose scanner outcomes to operators and validation
artifacts without letting notification side effects block cycle completion.

Output: notification sender interfaces, service integration, validation-index
updates, full scanner test coverage, and phase-ready verification evidence.
</objective>

<execution_context>
@/Users/rudlord/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rudlord/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-hermes-scanner/04-CONTEXT.md
@.planning/phases/04-hermes-scanner/04-RESEARCH.md
@.planning/phases/04-hermes-scanner/04-PATTERNS.md
@.planning/phases/04-hermes-scanner/04-VALIDATION.md
@.planning/phases/04-hermes-scanner/04-03-PLAN.md
@src/copysnipin/repositories/notifications.py
@src/copysnipin/repositories/heartbeats.py
@src/copysnipin/repositories/validation.py
@docs/validation-index.md
@docs/validation-hermes-scanner.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add notification sender protocols and fakes</name>
  <files>src/copysnipin/hermes_scanner/notifications.py, tests/copysnipin/test_scanner_notifications.py</files>
  <read_first>
    - src/copysnipin/repositories/notifications.py
    - src/copysnipin/config.py
    - docs/validation-hermes-scanner.md
  </read_first>
  <behavior>
    - Sender interfaces support Discord and Telegram independently.
    - Default tests use fake senders.
    - Sender errors are redacted before persistence.
    - Notification payloads contain wallet address, qualification metrics, and source period, but no secrets.
  </behavior>
  <action>
    Implement notification sender protocols and dispatch helpers. Keep real network delivery behind injectable sender implementations; default scanner service tests should not call external services. Use `NotificationRepository.record_notification_attempt()` for durable attempt records.
  </action>
  <verify>
    <automated>uv run pytest tests/copysnipin/test_scanner_notifications.py -q</automated>
    <automated>uv run ruff check src/copysnipin/hermes_scanner/notifications.py tests/copysnipin/test_scanner_notifications.py</automated>
  </verify>
  <acceptance_criteria>
    Tests cover Discord and Telegram success/failure paths without live webhooks or bot tokens.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate post-persistence alerts into scanner service</name>
  <files>src/copysnipin/hermes_scanner/service.py, tests/copysnipin/test_scanner_service.py, tests/copysnipin/test_scanner_notifications.py</files>
  <read_first>
    - src/copysnipin/hermes_scanner/service.py
    - src/copysnipin/hermes_scanner/notifications.py
    - src/copysnipin/repositories/notifications.py
  </read_first>
  <behavior>
    - Alerts are attempted only after qualifying wallet data and evidence persistence succeed.
    - Alert failures do not fail the cycle.
    - Cycle summaries and heartbeats include notification failure counts and degraded status when applicable.
  </behavior>
  <action>
    Wire notification dispatch into the scanner service after persistence. Add tests that force persistence failure before notification, notification failure after persistence, and mixed Discord/Telegram results. Record durable notification attempts and redacted failure diagnostics.
  </action>
  <verify>
    <automated>uv run pytest tests/copysnipin/test_scanner_service.py tests/copysnipin/test_scanner_notifications.py -q</automated>
  </verify>
  <acceptance_criteria>
    Tests cover `VAL-SCAN-018`, `VAL-SCAN-019`, and `VAL-SCAN-020`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update validation index and run full gates</name>
  <files>docs/validation-index.md, tests/copysnipin/test_validation_index.py, .planning/phases/04-hermes-scanner/04-04-SUMMARY.md</files>
  <read_first>
    - docs/validation-index.md
    - docs/validation-hermes-scanner.md
    - tests/copysnipin/test_validation_index.py
  </read_first>
  <behavior>
    - Every `VAL-SCAN-*` row remains exactly once in `docs/validation-index.md`.
    - Evidence paths reference the new Phase 04 automated tests or explicit manual/opt-in checks.
    - Rows should not claim live provider, Redis, PostgreSQL, Discord, or Telegram validation unless those checks were actually run.
  </behavior>
  <action>
    Update `docs/validation-index.md` scanner rows from pending placeholders to concrete evidence entries created in Phase 04. Keep the validation-index test strict about exact coverage and duplicates. Run the full final command gate and secret-pattern scan.
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
    <automated>uv run mypy src/</automated>
    <automated>uv run ruff check .</automated>
    <automated>uv run ruff format --check .</automated>
    <automated>gsd-sdk query verify.schema-drift 04</automated>
  </verify>
  <acceptance_criteria>
    Full checks pass, secret scan has no matches, and validation-index scanner rows cite concrete evidence without duplicates.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Scanner service -> Discord/Telegram senders | Durable scanner findings leave the process as notification payloads. |
| Notification sender errors -> database/logs | Webhook or bot failures can contain secret-bearing values. |
| Validation evidence -> project docs | Automated and manual validation claims become canonical project state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-04-01 | I | notification payloads | mitigate | Send only wallet metrics/source summaries and never include webhook URLs, bot tokens, or config secrets. |
| T-04-04-02 | D | notification dispatch | mitigate | Isolate notification failures from scanner cycle success and record degraded status. |
| T-04-04-03 | R | `docs/validation-index.md` | mitigate | Keep exact one-row `VAL-SCAN-*` coverage and cite only evidence that was actually run. |

</threat_model>

<verification>
Run:
- `uv run pytest -q`
- `uv run mypy src/`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `gsd-sdk query verify.schema-drift 04`
- secret-pattern scan over `.planning/phases/04-hermes-scanner`, `docs`, `src`, `tests`, `.env.example`, and `.factory`, excluding generated `__pycache__`.
</verification>

<success_criteria>
Plan 04 is complete when scanner alerts are isolated and durable, scanner
validation-index rows cite concrete evidence, and the full project gate passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-hermes-scanner/04-04-SUMMARY.md`.
</output>
