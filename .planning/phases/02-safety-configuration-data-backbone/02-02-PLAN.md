---
phase: 02-safety-configuration-data-backbone
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .env.example
  - .factory/library/environment.md
  - .factory/services.yaml
  - src/copysnipin/config.py
  - src/copysnipin/safety.py
  - tests/copysnipin/test_zero_execution.py
  - tests/copysnipin/test_safety_scaffold.py
  - tests/copysnipin/test_environment_contract.py
autonomous: true
requirements:
  - SAFE-03
  - SAFE-04
  - SAFE-05
must_haves:
  truths:
    - "The active source/config/factory surface remains zero-execution by default."
    - "Active environment variables agree across code, examples, factory defaults, and docs."
    - "Execution-adjacent variables are classified as disabled or future scope and never active startup requirements."
  artifacts:
    - path: "src/copysnipin/safety.py"
      provides: "Zero-execution policy constants and scan helpers"
      exports: ["BANNED_EXECUTION_TOKENS", "scan_zero_execution"]
    - path: "tests/copysnipin/test_zero_execution.py"
      provides: "Regression coverage that active code/config has no execution-capable paths"
    - path: "tests/copysnipin/test_environment_contract.py"
      provides: "Environment contract drift coverage"
    - path: ".factory/library/environment.md"
      provides: "Human-readable active/disabled environment contract"
  key_links:
    - from: "tests/copysnipin/test_zero_execution.py"
      to: "src/copysnipin/safety.py"
      via: "policy-driven source/config scan"
      pattern: "scan_zero_execution"
    - from: "tests/copysnipin/test_environment_contract.py"
      to: "src/copysnipin/config.py"
      via: "active/future env classification assertions"
      pattern: "ActiveSettings|FutureScopeSetting"
    - from: ".factory/services.yaml"
      to: "src/copysnipin/config.py"
      via: "inline defaults match active settings"
      pattern: "DATABASE_URL|REDIS_URL"
---

<objective>
Create the zero-execution and environment-contract guardrail.

Purpose: The project must make unsafe execution vocabulary and environment drift
visible before provider workers, persistence flows, or operator surfaces are
implemented.

Output: reusable zero-execution scan helpers, reconciled environment examples
and factory defaults, and focused safety/contract tests.
</objective>

<execution_context>
@/Users/rudlord/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rudlord/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-safety-configuration-data-backbone/02-CONTEXT.md
@.planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
@.planning/phases/02-safety-configuration-data-backbone/02-VALIDATION.md
@.planning/phases/02-safety-configuration-data-backbone/02-PATTERNS.md
@.planning/phases/02-safety-configuration-data-backbone/02-01-SUMMARY.md
@AGENTS.md

<interfaces>
Expected Plan 01 contracts:
```python
class ActiveSettings(BaseSettings): ...
class FutureScopeSetting(BaseModel): ...
def load_settings() -> ActiveSettings: ...
```

The zero-execution scan must not flag its own policy declaration or the
explicit disabled/future settings classification from Plan 01. The allowed
self-reference surfaces are limited to:

- an explicit policy declaration span inside `src/copysnipin/safety.py`;
- one exact `FutureScopeSetting` classification block or data structure in
  `src/copysnipin/config.py`.

Banned tokens must still fail in `ActiveSettings`, routes, commands, imports,
factory service defaults, and unclassified source text.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote zero-execution policy to reusable source/config scan</name>
  <files>src/copysnipin/safety.py, tests/copysnipin/test_zero_execution.py, tests/copysnipin/test_safety_scaffold.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-safety-configuration-data-backbone/02-CONTEXT.md
    - .planning/phases/02-safety-configuration-data-backbone/02-PATTERNS.md
    - tests/copysnipin/test_safety_scaffold.py
    - .factory/services.yaml
    - pyproject.toml
  </read_first>
  <behavior>
    - Active source files, routes, console scripts, and factory commands contain no route/command/import/config path that can sign, submit, cancel, bridge, approve, fund, or execute real trades per SAFE-03 and D-04.
    - The policy token list may contain banned terms only inside a narrow `src/copysnipin/safety.py` declaration block such as `ZERO_EXECUTION_POLICY_DECLARATION_START` / `ZERO_EXECUTION_POLICY_DECLARATION_END`.
    - The disabled/future settings classification may contain execution-adjacent names only inside one exact `src/copysnipin/config.py` `FutureScopeSetting` block or data structure.
    - Tests explicitly prove no active source/config file except the safety policy declaration and the marked future-scope classification contains banned execution tokens.
  </behavior>
  <action>
    Create `copysnipin.safety` with typed constants and scan helpers for banned execution-capable tokens per D-04. Include tokens covering private keys, signers, order placement/cancellation, allowances, funding, bridge/deposit/withdraw, relayers, Jito, LaserStream, and live-funded validation. Implement the self-match fix explicitly: strip only the marked policy declaration block from `src/copysnipin/safety.py` before scanning, and strip only the exact marked `FutureScopeSetting` disabled/future classification block or data structure from `src/copysnipin/config.py` before scanning. Do not allow those tokens elsewhere in `config.py`, especially in `ActiveSettings`. Replace the ad hoc logic in `test_safety_scaffold.py` with imports from `copysnipin.safety`, and add `test_zero_execution.py` scanning `src/copysnipin`, `pyproject.toml`, and `.factory/services.yaml`.
  </action>
  <verify>
    <automated>uv run pytest tests/copysnipin/test_zero_execution.py tests/copysnipin/test_safety_scaffold.py -q</automated>
    <automated>uv run ruff check src/copysnipin/safety.py tests/copysnipin/test_zero_execution.py tests/copysnipin/test_safety_scaffold.py</automated>
  </verify>
  <acceptance_criteria>
    Static safety tests fail if active code/config gains execution-capable vocabulary or imports; the scan does not fail only because of its own token declaration or the exact disabled/future settings classification block; existing scaffold entry point tests remain compatible; no provider polling or execution code is introduced.
  </acceptance_criteria>
  <done>Static safety tests fail if active code/config gains execution-capable vocabulary or imports; the scan does not fail only because of its own token declaration or the exact disabled/future settings classification block; existing scaffold entry point tests remain compatible; no provider polling or execution code is introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile active and disabled environment contracts</name>
  <files>.env.example, .factory/library/environment.md, .factory/services.yaml, src/copysnipin/config.py, tests/copysnipin/test_environment_contract.py</files>
  <read_first>
    - .planning/phases/02-safety-configuration-data-backbone/02-CONTEXT.md
    - .planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
    - .env.example
    - .factory/library/environment.md
    - .factory/services.yaml
    - docs/validation-contract.md
  </read_first>
  <behavior>
    - `.env.example`, `.factory/library/environment.md`, `.factory/services.yaml`, and `ActiveSettings` agree on active runtime variables per SAFE-04.
    - Helius, Solana private keys, LaserStream, Jito, live-funded validation, and execution-adjacent fields are classified as disabled/future scope per SAFE-05 and D-02.
    - Factory inline defaults use active runtime variables only.
  </behavior>
  <action>
    Update `.env.example` and `.factory/library/environment.md` to separate active runtime variables from disabled/future-scope variables. Add any missing active variables required by validation docs and Phase 02 context, including `PYTH_ASSETS` and `SIMULATION_SEED_USD`, while keeping provider behavior non-operative in this phase. Keep execution-adjacent placeholders documented only as disabled/future scope with no real secrets. Align `.factory/services.yaml` inline defaults with active settings only. Add `test_environment_contract.py` that parses `ActiveSettings`, `.env.example`, environment docs, and service YAML text to catch drift.
  </action>
  <verify>
    <automated>uv run pytest tests/copysnipin/test_environment_contract.py -q</automated>
    <automated>uv run pytest tests/copysnipin/test_zero_execution.py tests/copysnipin/test_environment_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    Active env variables match across code/docs/examples/factory defaults; disabled/future variables are clearly classified and not required by startup; no real `.env` contents are read; no secret values are committed.
  </acceptance_criteria>
  <done>Active env variables match across code/docs/examples/factory defaults; disabled/future variables are clearly classified and not required by startup; no real `.env` contents are read; no secret values are committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source/config -> runtime capability | Static project text can accidentally introduce execution affordances. |
| docs/examples/factory -> operator runtime | Example config can imply unsafe active capability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | E | `copysnipin.safety` and factory scripts | mitigate | Static scan active source/routes/scripts for execution-capable terms and imports, with a narrow policy declaration exclusion. |
| T-02-05 | S/T | `.env.example`, environment docs, services YAML | mitigate | Classify disabled/future env variables separately from active settings and test contract drift. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/copysnipin/test_zero_execution.py tests/copysnipin/test_safety_scaffold.py tests/copysnipin/test_environment_contract.py -q`
- `uv run pytest tests/copysnipin -q`
- `uv run mypy src/`
- `uv run ruff check .`
</verification>

<success_criteria>
Plan 02 is complete when zero-execution and environment-contract tests pass,
the scan avoids its own declaration self-match, and no execution/provider
behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-safety-configuration-data-backbone/02-02-SUMMARY.md`
</output>
