---
phase: 02-safety-configuration-data-backbone
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pyproject.toml
  - uv.lock
  - alembic.ini
  - src/copysnipin/db/__init__.py
  - src/copysnipin/db/models.py
  - src/copysnipin/db/session.py
  - src/copysnipin/db/migrations/env.py
  - src/copysnipin/db/migrations/script.py.mako
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "Developer can import SQLAlchemy metadata, session factories, and Alembic environment code without opening provider connections."
    - "Alembic target metadata is wired to the project declarative Base created in this plan."
    - "Session factories derive database connectivity from typed ActiveSettings rather than ad hoc environment reads."
  artifacts:
    - path: "src/copysnipin/db/models.py"
      provides: "Declarative Base and deterministic metadata naming convention"
      contains: "Base"
    - path: "src/copysnipin/db/session.py"
      provides: "Engine and session factory helpers"
      exports: ["create_engine_from_settings", "create_session_factory"]
    - path: "src/copysnipin/db/migrations/env.py"
      provides: "Alembic environment wired to project metadata"
      contains: "target_metadata"
  key_links:
    - from: "src/copysnipin/db/migrations/env.py"
      to: "src/copysnipin/db/models.py"
      via: "Alembic target metadata"
      pattern: "target_metadata"
    - from: "src/copysnipin/db/session.py"
      to: "src/copysnipin/config.py"
      via: "database URL from ActiveSettings"
      pattern: "ActiveSettings|database_url"
---

<objective>
Create the database dependency, session, and Alembic substrate.

Purpose: Later schema and repository plans need importable SQLAlchemy/Alembic
infrastructure before they define the durable CopySnipIn table families.

Output: SQLAlchemy/Alembic dependencies, DB package skeleton, declarative Base,
session factories, and Alembic environment files.
</objective>

<execution_context>
@/Users/rudlord/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rudlord/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-safety-configuration-data-backbone/02-CONTEXT.md
@.planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
@.planning/phases/02-safety-configuration-data-backbone/02-VALIDATION.md
@.planning/phases/02-safety-configuration-data-backbone/02-PATTERNS.md
@.planning/phases/02-safety-configuration-data-backbone/02-01-SUMMARY.md

<interfaces>
Expected Plan 01 contracts:
```python
class ActiveSettings(BaseSettings):
    database_url: str
    redis_url: str

def load_settings() -> ActiveSettings: ...
```

Plan 03 must import the settings contract rather than rereading environment
variables directly. Plan 04 will extend `models.py` with table models, so keep
this plan focused on the shared Base and migration/session substrate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SQLAlchemy and Alembic dependencies</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - .planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
    - .planning/phases/02-safety-configuration-data-backbone/02-PATTERNS.md
    - pyproject.toml
  </read_first>
  <behavior>
    - SQLAlchemy, Alembic, and psycopg are locked through `uv`, not hand-edited dependency files.
    - The package remains Python 3.13+ and compatible with existing pytest, mypy, and ruff tooling.
  </behavior>
  <action>
    Add `SQLAlchemy>=2.0,<2.1`, `alembic>=1.18,<2`, and `psycopg[binary]>=3.3,<4` with `uv add`. Do not add repository, provider, scanner, tracker, simulator, Pyth, API, or dashboard behavior in this plan.
  </action>
  <verify>
    <automated>uv run python -c "import sqlalchemy, alembic, psycopg; print(sqlalchemy.__version__)"</automated>
    <automated>uv run ruff check pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    Dependency manifest and lockfile include the database substrate packages through uv-managed changes.
  </acceptance_criteria>
  <done>Dependency manifest and lockfile include the database substrate packages through uv-managed changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DB Base, session factories, and Alembic environment</name>
  <files>alembic.ini, src/copysnipin/db/__init__.py, src/copysnipin/db/models.py, src/copysnipin/db/session.py, src/copysnipin/db/migrations/env.py, src/copysnipin/db/migrations/script.py.mako</files>
  <read_first>
    - .planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
    - .planning/phases/02-safety-configuration-data-backbone/02-PATTERNS.md
    - .planning/phases/02-safety-configuration-data-backbone/02-01-SUMMARY.md
    - src/copysnipin/config.py
  </read_first>
  <behavior>
    - SQLAlchemy uses deterministic naming conventions suitable for Alembic migrations.
    - Session factories are created from `ActiveSettings.database_url`, not ad hoc environment reads.
    - Alembic can import project metadata without opening provider connections or requiring live DB in default tests.
  </behavior>
  <action>
    Create `copysnipin.db` with declarative `Base` in `models.py`, a deterministic metadata naming convention, `create_engine_from_settings()`, and `create_session_factory()`. Add Alembic config under `alembic.ini` and `src/copysnipin/db/migrations/`, with `env.py` importing `Base.metadata` from `copysnipin.db.models` and using the Plan 01 settings loader only for online migrations. Do not create durable table models or the baseline migration here; those belong to Plan 04. Do not run a live migration as part of default verification.
  </action>
  <verify>
    <automated>uv run python -c "from copysnipin.db.models import Base; from copysnipin.db.session import create_session_factory; print(Base.metadata.naming_convention)"</automated>
    <automated>uv run mypy src/</automated>
  </verify>
  <acceptance_criteria>
    DB helpers import cleanly; Alembic metadata is wired; no default test attempts a network connection.
  </acceptance_criteria>
  <done>DB helpers import cleanly; Alembic metadata is wired; no default test attempts a network connection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings -> database engine | Configured DSN crosses into SQLAlchemy engine creation. |
| migration code -> live PostgreSQL | Alembic can change durable schema when explicitly run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | T | Alembic environment and DB metadata | mitigate | Use deterministic metadata naming conventions and wire Alembic to a single project Base. |
| T-02-07 | I | DB URLs in migration/session errors | mitigate | Reuse Plan 01 redaction for settings errors; do not echo real `.env` values. |
| T-02-08 | D | Default test suite | mitigate | Keep live PostgreSQL migration checks opt-in/manual, not required by `uv run pytest`. |
</threat_model>

<verification>
Run:
- `uv run python -c "from copysnipin.db.models import Base; from copysnipin.db.session import create_session_factory; print(Base.metadata.naming_convention)"`
- `uv run pytest tests/copysnipin -q`
- `uv run mypy src/`
- `uv run ruff check .`
</verification>

<success_criteria>
Plan 03 is complete when the database substrate imports cleanly, Alembic points
at project metadata, and default verification remains independent from live
PostgreSQL.
</success_criteria>

<output>
After completion, create `.planning/phases/02-safety-configuration-data-backbone/02-03-SUMMARY.md`
</output>
