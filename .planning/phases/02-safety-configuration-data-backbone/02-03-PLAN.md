---
phase: 02-safety-configuration-data-backbone
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - pyproject.toml
  - uv.lock
  - src/copysnipin/coordination/__init__.py
  - src/copysnipin/coordination/redis_locks.py
  - src/copysnipin/repositories/__init__.py
  - src/copysnipin/repositories/heartbeats.py
  - src/copysnipin/repositories/validation.py
  - docs/validation-index.md
  - tests/copysnipin/test_redis_locks.py
  - tests/copysnipin/test_heartbeats.py
  - tests/copysnipin/test_validation_index.py
autonomous: true
requirements:
  - DATA-03
  - DATA-04
  - VAL-01
must_haves:
  truths:
    - "Redis coordination provides owner-token locks for overlap prevention without storing durable business state."
    - "Component heartbeat writes persist success/error/degraded/stale freshness state in PostgreSQL-backed tables."
    - "Every validation assertion in `docs/validation-*.md` has exactly one owner/evidence row in a committed validation index."
  artifacts:
    - path: "src/copysnipin/coordination/redis_locks.py"
      provides: "Owner-token Redis lock abstraction"
      exports: ["RedisLockConfig", "OwnerTokenRedisLock"]
    - path: "src/copysnipin/repositories/heartbeats.py"
      provides: "Heartbeat repository primitives"
      exports: ["HeartbeatRepository"]
    - path: "src/copysnipin/repositories/validation.py"
      provides: "Validation evidence repository primitives"
      exports: ["ValidationEvidenceRepository"]
    - path: "docs/validation-index.md"
      provides: "VAL-* assertion ownership/evidence matrix"
  key_links:
    - from: "src/copysnipin/coordination/redis_locks.py"
      to: "src/copysnipin/config.py"
      via: "Redis URL and scanner interval settings consumed by callers"
      pattern: "redis"
    - from: "src/copysnipin/repositories/heartbeats.py"
      to: "component_heartbeats"
      via: "SQLAlchemy model/table"
      pattern: "ComponentHeartbeat"
    - from: "tests/copysnipin/test_validation_index.py"
      to: "docs/validation-index.md"
      via: "parse all validation docs and assert exactly one row per ID"
      pattern: "VAL-"
---

<objective>
Finish Phase 02 coordination, heartbeat, and validation ownership substrate.

Purpose: Workers need a safe coordination primitive, durable component status,
and validation evidence ownership before scanner/tracker/simulator/Pyth/API/UI
phases start producing observable behavior.

Output: Redis owner-token lock abstraction, heartbeat and validation evidence
repositories, a committed validation index, and coverage tests.
</objective>

<execution_context>
@/Users/rudlord/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rudlord/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-safety-configuration-data-backbone/02-CONTEXT.md
@.planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
@.planning/phases/02-safety-configuration-data-backbone/02-VALIDATION.md
@.planning/phases/02-safety-configuration-data-backbone/02-PATTERNS.md
@.planning/phases/02-safety-configuration-data-backbone/02-01-SUMMARY.md
@.planning/phases/02-safety-configuration-data-backbone/02-02-SUMMARY.md
@docs/validation-contract.md
@docs/validation-hermes-scanner.md
@docs/validation-tracker-simulation.md

<interfaces>
Expected Plan 02 contracts:
```python
from copysnipin.db.models import ComponentHeartbeat, ValidationEvidence
from copysnipin.db.session import create_session_factory
```

Plan 03 must store durable heartbeat and validation evidence in PostgreSQL
models from Plan 02. Redis must be used only for owner-token locks and
short-lived coordination state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add owner-token Redis coordination lock</name>
  <files>pyproject.toml, uv.lock, src/copysnipin/coordination/__init__.py, src/copysnipin/coordination/redis_locks.py, tests/copysnipin/test_redis_locks.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-safety-configuration-data-backbone/02-CONTEXT.md
    - .planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
    - docs/validation-hermes-scanner.md
    - src/copysnipin/config.py
  </read_first>
  <behavior>
    - Lock acquisition uses owner tokens, finite TTL, and non-blocking acquisition for scanner overlap prevention per DATA-03.
    - Release succeeds only for the owner token.
    - Redis API does not expose durable wallet, trade, watermark, heartbeat, simulation, price, or validation-evidence storage.
  </behavior>
  <action>
    Add `redis>=7.4,<8` and dev `fakeredis>=2.35,<3` with uv. Create `copysnipin.coordination.redis_locks` wrapping redis-py lock semantics with a typed `RedisLockConfig` and `OwnerTokenRedisLock`. Require a finite TTL, explicit owner token, lock key such as `hermes:scanner:lock`, and `blocking=False`. Add tests with fakeredis or a narrow fake proving acquire, competing acquire rejection, token-protected release, TTL requirement, and absence of durable business-state APIs. Do not require live Redis in default tests.
  </action>
  <verify>
    <automated>uv run pytest tests/copysnipin/test_redis_locks.py -q</automated>
    <automated>uv run mypy src/</automated>
  </verify>
  <acceptance_criteria>
    Redis lock abstraction satisfies DATA-03 with owner tokens and TTL; tests pass using fake Redis; no durable state is stored in Redis; no scanner loop behavior is implemented.
  </acceptance_criteria>
  <done>Redis lock abstraction satisfies DATA-03 with owner tokens and TTL; tests pass using fake Redis; no durable state is stored in Redis; no scanner loop behavior is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add heartbeat and validation evidence repositories</name>
  <files>src/copysnipin/repositories/__init__.py, src/copysnipin/repositories/heartbeats.py, src/copysnipin/repositories/validation.py, tests/copysnipin/test_heartbeats.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
    - .planning/phases/02-safety-configuration-data-backbone/02-02-SUMMARY.md
    - src/copysnipin/db/models.py
    - src/copysnipin/db/session.py
    - src/copysnipin/security/redaction.py
  </read_first>
  <behavior>
    - Heartbeat rows record component name, last success, last error, degraded/stale state, and freshness timestamps for scanner, tracker, simulator, Pyth feed, API, and dashboard-visible systems per DATA-04.
    - Error snippets are redacted before persistence.
    - Validation evidence repository writes assertion ID, owner phase, evidence path/command, state, and timestamps without replacing `docs/validation-index.md`.
  </behavior>
  <action>
    Create repository classes that accept a SQLAlchemy session/session factory from Plan 02 and use explicit transaction boundaries. Implement heartbeat upsert methods for success, error, degraded, and stale transitions against `component_heartbeats`, using the Plan 01 redactor for error snippets. Implement validation evidence upsert primitives against `validation_evidence`. Add tests for heartbeat state transitions and redaction using SQLAlchemy local test sessions or statement/metadata inspection that does not require live PostgreSQL.
  </action>
  <verify>
    <automated>uv run pytest tests/copysnipin/test_heartbeats.py -q</automated>
    <automated>uv run pytest tests/copysnipin/test_repositories.py tests/copysnipin/test_heartbeats.py -q</automated>
  </verify>
  <acceptance_criteria>
    Heartbeat repository satisfies DATA-04; validation evidence repository can persist VAL ownership/evidence observations later; errors are redacted before persistence; default tests do not require live PostgreSQL.
  </acceptance_criteria>
  <done>Heartbeat repository satisfies DATA-04; validation evidence repository can persist VAL ownership/evidence observations later; errors are redacted before persistence; default tests do not require live PostgreSQL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create validation index and exact coverage test</name>
  <files>docs/validation-index.md, tests/copysnipin/test_validation_index.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-safety-configuration-data-backbone/02-CONTEXT.md
    - .planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
    - docs/validation-contract.md
    - docs/validation-hermes-scanner.md
    - docs/validation-tracker-simulation.md
  </read_first>
  <behavior>
    - Every `VAL-DASH-*`, `VAL-PYTH-*`, `VAL-CROSS-*`, `VAL-SCAN-*`, `VAL-TRACK-*`, and `VAL-SIM-*` heading in validation docs appears exactly once in `docs/validation-index.md`.
    - Each index row has owner phase, automation/manual status, evidence command or path, and current state per VAL-01 and D-08.
    - Rows for later phases can be pending, but ownership and evidence target must be explicit.
  </behavior>
  <action>
    Create `docs/validation-index.md` as a Markdown table with one row per validation assertion parsed from `docs/validation-*.md`. Assign owner phases based on roadmap responsibility: scanner IDs to Phase 4 except foundation/schema evidence owned by Phase 2 when appropriate, tracker IDs to Phase 5, simulator IDs to Phase 6, Pyth IDs to Phase 7, dashboard/API/cross-flow IDs to Phase 8 unless a durable substrate row is clearly Phase 2-owned. Add `test_validation_index.py` that extracts all VAL IDs from validation docs and asserts exact one-row coverage, required columns, non-empty owner phase/status/evidence/current state, and no duplicate IDs.
  </action>
  <verify>
    <automated>uv run pytest tests/copysnipin/test_validation_index.py -q</automated>
    <automated>uv run pytest tests/copysnipin/test_redis_locks.py tests/copysnipin/test_heartbeats.py tests/copysnipin/test_validation_index.py -q</automated>
  </verify>
  <acceptance_criteria>
    `docs/validation-index.md` satisfies VAL-01 for all validation IDs; coverage test fails on missing/duplicate/stale rows; no validation behavior from later phases is implemented here.
  </acceptance_criteria>
  <done>`docs/validation-index.md` satisfies VAL-01 for all validation IDs; coverage test fails on missing/duplicate/stale rows; no validation behavior from later phases is implemented here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker process -> Redis | Coordination lock requests cross into short-lived Redis state. |
| component errors -> PostgreSQL | Error snippets cross into durable heartbeat rows. |
| validation docs -> index/evidence store | Human-readable contract IDs become executable coverage expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | S/T | `OwnerTokenRedisLock` | mitigate | Require explicit owner tokens, finite TTL, non-blocking acquire, and token-protected release. |
| T-02-10 | I | `HeartbeatRepository` | mitigate | Redact error snippets before durable persistence. |
| T-02-11 | R | `docs/validation-index.md` | mitigate | Test exact one-row VAL coverage and required ownership/evidence fields. |
| T-02-12 | D | Redis dependency | accept | Default tests use fake Redis; live Redis verification remains opt-in/manual per phase constraint. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/copysnipin/test_redis_locks.py tests/copysnipin/test_heartbeats.py tests/copysnipin/test_validation_index.py -q`
- `uv run pytest tests/copysnipin -q`
- `uv run mypy src/`
- `uv run ruff check .`
- `uv run ruff format --check .`

Optional live check, only when local Redis is intentionally available:
- `redis-cli ping`
- a small `uv run python -c` smoke script acquiring and releasing `hermes:scanner:lock`
</verification>

<success_criteria>
Plan 03 is complete when DATA-03, DATA-04, and VAL-01 are covered by code,
docs, and automated tests; default verification does not require live Redis or
PostgreSQL; and no later-phase provider/worker/UI behavior is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/02-safety-configuration-data-backbone/02-03-SUMMARY.md`
</output>
