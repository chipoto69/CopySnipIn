---
phase: 02-safety-configuration-data-backbone
plan: 04
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - src/copysnipin/repositories/__init__.py
  - src/copysnipin/repositories/wallets.py
  - src/copysnipin/repositories/trades.py
  - src/copysnipin/repositories/watermarks.py
  - src/copysnipin/repositories/simulations.py
  - src/copysnipin/repositories/notifications.py
  - src/copysnipin/repositories/validation.py
  - tests/copysnipin/test_repositories.py
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - "Repository methods use explicit transaction boundaries for wallet discovery, trade ingestion, watermarks, simulation writes, notifications, and validation evidence."
    - "Repository idempotency is backed by schema-level uniqueness and PostgreSQL ON CONFLICT behavior."
    - "Default tests prove conflict targets and transaction boundaries without requiring live PostgreSQL."
  artifacts:
    - path: "src/copysnipin/repositories/wallets.py"
      provides: "Wallet discovery idempotent repository methods"
      exports: ["WalletRepository"]
    - path: "src/copysnipin/repositories/trades.py"
      provides: "Canonical trade ingestion repository methods"
      exports: ["TradeRepository"]
    - path: "src/copysnipin/repositories/watermarks.py"
      provides: "Durable wallet watermark repository methods"
      exports: ["WatermarkRepository"]
    - path: "src/copysnipin/repositories/simulations.py"
      provides: "Simulation portfolio/trade repository methods"
      exports: ["SimulationRepository"]
    - path: "src/copysnipin/repositories/notifications.py"
      provides: "Notification idempotency repository methods"
      exports: ["NotificationRepository"]
    - path: "src/copysnipin/repositories/validation.py"
      provides: "Validation evidence repository methods"
      exports: ["ValidationEvidenceRepository"]
  key_links:
    - from: "src/copysnipin/repositories/*.py"
      to: "src/copysnipin/db/models.py"
      via: "SQLAlchemy model imports and unique conflict targets"
      pattern: "on_conflict_do_"
    - from: "src/copysnipin/repositories/*.py"
      to: "src/copysnipin/db/session.py"
      via: "sessionmaker.begin transaction boundaries"
      pattern: "begin\\("
    - from: "tests/copysnipin/test_repositories.py"
      to: "src/copysnipin/repositories/*.py"
      via: "statement and transaction contract inspection"
      pattern: "WalletRepository|TradeRepository|WatermarkRepository|SimulationRepository|NotificationRepository"
---

<objective>
Create concrete idempotent repository primitives for DATA-02.

Purpose: Later scanner, tracker, simulator, notification, and validation
workers need restart-safe persistence methods, not generic transaction helpers.

Output: repository modules for wallets, trades, watermarks, simulations,
notifications, validation evidence, and tests proving transaction/upsert
contracts.
</objective>

<execution_context>
@/Users/rudlord/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rudlord/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-safety-configuration-data-backbone/02-CONTEXT.md
@.planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
@.planning/phases/02-safety-configuration-data-backbone/02-VALIDATION.md
@.planning/phases/02-safety-configuration-data-backbone/02-PATTERNS.md
@.planning/phases/02-safety-configuration-data-backbone/02-03-SUMMARY.md

<interfaces>
Expected Plan 03 contracts:
```python
from copysnipin.db.models import (
    Notification,
    SimulationPortfolio,
    SimulatedTrade,
    Trade,
    ValidationEvidence,
    Wallet,
    WalletWatermark,
)
from copysnipin.db.session import create_session_factory
```

Repositories must use Plan 03 models and session factories. They must not store
business state in Redis or in process-global memory.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add wallet, trade, and watermark repositories</name>
  <files>src/copysnipin/repositories/__init__.py, src/copysnipin/repositories/wallets.py, src/copysnipin/repositories/trades.py, src/copysnipin/repositories/watermarks.py, tests/copysnipin/test_repositories.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
    - .planning/phases/02-safety-configuration-data-backbone/02-03-SUMMARY.md
    - src/copysnipin/db/models.py
    - src/copysnipin/db/session.py
    - tests/copysnipin/test_db_metadata.py
  </read_first>
  <behavior>
    - `WalletRepository.upsert_discovered_wallet()` is idempotent by canonical wallet address and preserves lifecycle fields needed by scanner phases.
    - `TradeRepository.ingest_trade()` is idempotent by provider/deduplication identity while preserving distinct split trades allowed by validation contracts.
    - `WatermarkRepository.upsert_wallet_watermark()` and `advance_wallet_watermark()` persist durable per-wallet checkpoints in PostgreSQL.
    - Each write method runs inside `sessionmaker.begin()` or an equivalent explicit transaction boundary.
  </behavior>
  <action>
    Create repository classes for wallets, trades, and watermarks using SQLAlchemy PostgreSQL `insert().on_conflict_do_update()` or `on_conflict_do_nothing()` against the unique constraints from Plan 03. Methods must accept typed inputs that match existing schema columns and return the persisted row identity or a small typed result object. Add tests that inspect compiled PostgreSQL statements/conflict targets and prove each public write method uses a transaction boundary. Do not implement Polymarket polling, trade parsing, qualification math, or live database tests.
  </action>
  <verify>
    <automated>uv run pytest tests/copysnipin/test_repositories.py -q</automated>
    <automated>uv run ruff check src/copysnipin/repositories tests/copysnipin/test_repositories.py</automated>
  </verify>
  <acceptance_criteria>
    Wallet, trade, and watermark repositories expose concrete idempotent write methods with transaction boundaries and conflict targets aligned to schema uniqueness.
  </acceptance_criteria>
  <done>Wallet, trade, and watermark repositories expose concrete idempotent write methods with transaction boundaries and conflict targets aligned to schema uniqueness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add simulation, notification, and validation evidence repositories</name>
  <files>src/copysnipin/repositories/simulations.py, src/copysnipin/repositories/notifications.py, src/copysnipin/repositories/validation.py, tests/copysnipin/test_repositories.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
    - src/copysnipin/db/models.py
    - src/copysnipin/db/session.py
    - src/copysnipin/repositories/wallets.py
    - src/copysnipin/repositories/trades.py
    - src/copysnipin/repositories/watermarks.py
  </read_first>
  <behavior>
    - `SimulationRepository.upsert_portfolio()` is idempotent by strategy/portfolio identity.
    - `SimulationRepository.record_simulated_trade()` is idempotent by source trade and strategy/portfolio identity.
    - `NotificationRepository.record_notification_attempt()` is idempotent by notification idempotency key and records success/failure metadata without sending alerts.
    - `ValidationEvidenceRepository.upsert_evidence()` is idempotent by assertion/evidence identity and stores owner/status/evidence metadata.
    - Each write method runs inside `sessionmaker.begin()` or an equivalent explicit transaction boundary.
  </behavior>
  <action>
    Create repository classes for simulation portfolios/trades, notifications, and validation evidence using PostgreSQL upsert/no-op conflict behavior against Plan 03 unique constraints. Notification repository methods must only record notification attempts; they must not call Discord, Telegram, or other external services. Simulation repository methods must only persist paper-trading records; they must not implement sizing, PnL, or strategy math. Extend `test_repositories.py` to prove transaction boundaries, conflict targets, and absence of external provider/client calls.
  </action>
  <verify>
    <automated>uv run pytest tests/copysnipin/test_repositories.py -q</automated>
    <automated>uv run pytest tests/copysnipin/test_db_metadata.py tests/copysnipin/test_repositories.py -q</automated>
  </verify>
  <acceptance_criteria>
    Simulation, notification, and validation evidence repositories expose concrete idempotent write methods; notification methods do not send alerts; simulation methods do not perform accounting; tests pass without live PostgreSQL.
  </acceptance_criteria>
  <done>Simulation, notification, and validation evidence repositories expose concrete idempotent write methods; notification methods do not send alerts; simulation methods do not perform accounting; tests pass without live PostgreSQL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future provider/domain data -> SQL statements | Untrusted provider-shaped data will cross into persistence methods. |
| repository methods -> PostgreSQL | Repository writes can mutate durable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | R/T | Repository methods | mitigate | Require explicit transaction boundaries and schema-backed ON CONFLICT behavior for every DATA-02 write path. |
| T-02-10 | I | Notification repository | mitigate | Persist notification status only; do not call external webhook/token-bearing services. |
| T-02-11 | T | Simulation repository | mitigate | Persist paper-trading records only; do not implement live execution or provider behavior. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/copysnipin/test_repositories.py -q`
- `uv run pytest tests/copysnipin/test_db_metadata.py tests/copysnipin/test_repositories.py -q`
- `uv run mypy src/`
- `uv run ruff check .`
</verification>

<success_criteria>
Plan 04 is complete when DATA-02 is covered by concrete repository modules for
wallet discovery, trade ingestion, watermarks, simulations, notifications, and
validation evidence, all using transactions and schema-backed idempotency.
</success_criteria>

<output>
After completion, create `.planning/phases/02-safety-configuration-data-backbone/02-04-SUMMARY.md`
</output>
