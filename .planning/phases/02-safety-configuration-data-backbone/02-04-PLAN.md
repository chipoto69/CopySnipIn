---
phase: 02-safety-configuration-data-backbone
plan: 04
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - src/copysnipin/db/models.py
  - src/copysnipin/db/migrations/versions/02_baseline.py
  - tests/copysnipin/test_db_metadata.py
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "Developer can inspect SQLAlchemy metadata and Alembic migration coverage for all Phase 02 durable table families."
    - "Default tests prove schema names, uniqueness constraints, and indexes without requiring live PostgreSQL."
    - "The schema exposes uniqueness targets required by idempotent repository methods."
  artifacts:
    - path: "src/copysnipin/db/models.py"
      provides: "Phase 02 durable SQLAlchemy model metadata"
      contains: "Base"
    - path: "src/copysnipin/db/migrations/versions/02_baseline.py"
      provides: "Alembic baseline migration for required durable tables"
      exports: ["upgrade", "downgrade"]
    - path: "tests/copysnipin/test_db_metadata.py"
      provides: "Schema/migration coverage tests"
  key_links:
    - from: "src/copysnipin/db/migrations/versions/02_baseline.py"
      to: "src/copysnipin/db/models.py"
      via: "matching table/index/constraint definitions"
      pattern: "op\\.create_table|op\\.create_index"
    - from: "tests/copysnipin/test_db_metadata.py"
      to: "src/copysnipin/db/models.py"
      via: "metadata table inspection"
      pattern: "Base.metadata"
    - from: "tests/copysnipin/test_db_metadata.py"
      to: "src/copysnipin/db/migrations/versions/02_baseline.py"
      via: "migration source inspection"
      pattern: "02_baseline"
---

<objective>
Create the PostgreSQL schema baseline.

Purpose: Later scanner, tracker, simulator, Pyth, API, and dashboard phases need
durable tables and deterministic constraints before they can safely write
read-only and paper-trading state.

Output: SQLAlchemy 2.x model metadata, Alembic baseline migration, and schema
coverage tests.
</objective>

<execution_context>
@/Users/rudlord/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rudlord/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-safety-configuration-data-backbone/02-CONTEXT.md
@.planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
@.planning/phases/02-safety-configuration-data-backbone/02-VALIDATION.md
@.planning/phases/02-safety-configuration-data-backbone/02-PATTERNS.md
@.planning/phases/02-safety-configuration-data-backbone/02-03-SUMMARY.md

<interfaces>
Expected Plan 03 contracts:
```python
from copysnipin.db.models import Base
from copysnipin.db.session import create_session_factory
```

Plan 04 must extend the Plan 03 Base with concrete durable table models and keep
the baseline migration aligned with those models.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model required durable table families</name>
  <files>src/copysnipin/db/models.py, tests/copysnipin/test_db_metadata.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-safety-configuration-data-backbone/02-CONTEXT.md
    - .planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
    - .planning/phases/02-safety-configuration-data-backbone/02-03-SUMMARY.md
    - docs/validation-contract.md
    - docs/validation-hermes-scanner.md
    - docs/validation-tracker-simulation.md
  </read_first>
  <behavior>
    - Metadata covers wallets, scanner runs, qualification evidence, trades, watermarks, simulation portfolios, simulated trades, positions, price updates, correlations, notifications, component heartbeats, and validation evidence per DATA-01.
    - Canonical wallet identity is `wallets`; scanner/tracker/simulation tables link to it rather than creating competing wallet identity tables.
    - Uniqueness constraints exist for idempotent wallet discovery, trade ingestion, watermarks, simulation writes, notification records, component heartbeats, and validation evidence.
  </behavior>
  <action>
    Extend the Plan 03 `Base` with SQLAlchemy models for every DATA-01 table family using PostgreSQL-compatible column types, timestamp columns with timezone, explicit table names, and deterministic indexes/unique constraints. Include raw payload/evidence reference columns where later provider phases need storage, but do not implement provider parsing, scanner qualification math, tracker polling, simulation accounting, or Pyth WebSocket behavior. Start `test_db_metadata.py` by asserting required table names, key columns, and unique/index contracts from `Base.metadata`.
  </action>
  <verify>
    <automated>uv run pytest tests/copysnipin/test_db_metadata.py -q</automated>
    <automated>uv run ruff check src/copysnipin/db tests/copysnipin/test_db_metadata.py</automated>
  </verify>
  <acceptance_criteria>
    Every DATA-01 table family is represented in SQLAlchemy metadata with required uniqueness/index contracts.
  </acceptance_criteria>
  <done>Every DATA-01 table family is represented in SQLAlchemy metadata with required uniqueness/index contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add baseline migration and migration coverage tests</name>
  <files>src/copysnipin/db/migrations/versions/02_baseline.py, tests/copysnipin/test_db_metadata.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-safety-configuration-data-backbone/02-CONTEXT.md
    - .planning/phases/02-safety-configuration-data-backbone/02-RESEARCH.md
    - .planning/phases/02-safety-configuration-data-backbone/02-03-SUMMARY.md
    - src/copysnipin/db/models.py
  </read_first>
  <behavior>
    - Migration creates the same durable table families, uniqueness constraints, and indexes exposed by metadata.
    - Downgrade reverses created indexes/tables in dependency-safe order.
    - Default tests inspect migration coverage without applying it to live PostgreSQL.
  </behavior>
  <action>
    Create a hand-authored Alembic baseline revision `02_baseline.py` with matching `op.create_table`, `op.create_index`, and downgrade operations. Extend `test_db_metadata.py` to compare required table names and key uniqueness/index contracts against both metadata and the migration source. Keep live `alembic upgrade head` verification optional only.
  </action>
  <verify>
    <automated>uv run pytest tests/copysnipin/test_db_metadata.py -q</automated>
    <automated>uv run mypy src/</automated>
  </verify>
  <acceptance_criteria>
    Metadata and migration cover every DATA-01 table family; tests prove required table names and uniqueness/index contracts; no live PostgreSQL is required for default tests.
  </acceptance_criteria>
  <done>Metadata and migration cover every DATA-01 table family; tests prove required table names and uniqueness/index contracts; no live PostgreSQL is required for default tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration code -> live PostgreSQL | Alembic can create or drop durable tables when explicitly run. |
| future provider/domain data -> schema constraints | Later untrusted data will rely on schema uniqueness for idempotency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | T | Alembic baseline migration | mitigate | Use deterministic table/index/constraint names and metadata/migration coverage tests. |
| T-02-08 | D | Default test suite | mitigate | Keep live PostgreSQL migration checks opt-in/manual, not required by `uv run pytest`. |
| T-02-09 | R/T | Repository dependency contract | mitigate | Expose schema-level uniqueness targets that Plan 05 repository methods must use for ON CONFLICT behavior. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/copysnipin/test_db_metadata.py -q`
- `uv run pytest tests/copysnipin -q`
- `uv run mypy src/`
- `uv run ruff check .`

Optional live check, only when local PostgreSQL is intentionally available:
- `uv run alembic upgrade head`
- `psql "$DATABASE_URL" -c '\\dt'`
</verification>

<success_criteria>
Plan 04 is complete when SQLAlchemy metadata and Alembic migration cover all
DATA-01 durable table families and default verification remains independent
from live PostgreSQL.
</success_criteria>

<output>
After completion, create `.planning/phases/02-safety-configuration-data-backbone/02-04-SUMMARY.md`
</output>
